### Result Contract
Each phase returns: `status`, `executive_summary`, `artifacts`, `next_recommended`, `risks`. Show `executive_summary` to user, plus `risks` and `next_recommended` when present. Show `status` only on failure. Keep `artifacts` internal for orchestration.
